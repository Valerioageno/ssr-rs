{
  "name": "ssr-svelte-5",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:client": "vite build --config vite.client.config.js",
    "build:ssr": "vite build --config vite.ssr.config.js",
    "build": "pnpm run build:client && pnpm run build:ssr",
    "preview": "pnpm run build && cargo run"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "5.0.3",
    "svelte": "5.23.0",
    "vite": "6.2.1"
  }
}
